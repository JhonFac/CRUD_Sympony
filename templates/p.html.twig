{% extends 'base.html.twig' %}

{% block title %}Update - {{ name }}{% endblock %}

{% block body %}


<div class="row">
    <div class="col-sm-4">
    </div>
    <div class="col-sm-4">
        <div class="card">
            <div class="card-body">
                <h1>vas a editar a: {{ name }}</h1>
                <br>
                {{ form(formCategory) }}
                <br>
            </div>
        </div>
    </div>
</div>




{% endblock %}